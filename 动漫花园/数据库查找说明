# mydoc = mycol.find_one(myquery,max_time_ms=50000) # 查询所有符合条件的数据
# mydoc = mycol.count_documents(myquery)  # 查询所有符合条件的数据
# print('找到%d条数据。' % mydoc)
'''
if mydoc != 0:
    print('%s 数据已存在' % fabu_link)
else:
    mycol.insert_one(suju)



if mydoc != None:
    print('%s 数据已存在' % fabu_link)
else:
    mycol.insert_one(suju)

# 对查询到的数据进行循环计数
for i in mydoc:
    mongo_jisu = mongo_jisu + 1
    if mongo_jisu == 0:
        mycol.insert_one(suju)
    elif mongo_jisu > 0:
        print('%s 数据已存在' % fabu_link)
'''


1.设置列表页获取下载链接少于80则自动到内页获取

2.设置先验证数据是否存在，然后在进行抓取

19920124Zhy@.